$(function() {
    $("input[name='button']").on('click', (evt) => {
        evt.preventDefault();
        let name = $("input[name='name']").val();
        let password = $("input[name='password'").val();

        fetch
    })
});